import { bark } from './index'

describe('A pug came into the kitchen', () => {
  it('and stole an egg from the cook', async () => {
    // Preparation

    // Execution
    expect(bark()).toBe('bark')

    // Assertion
  })
})
